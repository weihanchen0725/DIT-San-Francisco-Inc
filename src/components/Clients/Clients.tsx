import clientClass from './Clients.module.scss';

const Clients = () => {
  return (
    <section id="clients" className={clientClass.clients}>
      Client List
    </section>
  );
};
export default Clients;
